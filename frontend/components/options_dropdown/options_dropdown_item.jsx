import React from 'react';

const OptionsDropdownItem = ({ action, message, handleClose }) => {
    const onClick = (e) => {
        e.preventDefault();

    }
    
    return (
        <li className='dropdown-item'>

        </li>
    )
}